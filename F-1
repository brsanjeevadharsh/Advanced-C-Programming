#include <stdio.h>
int add(int a, int b);
int main() {
    int num1, num2;
     printf("Enter the two numbers: ");
    scanf("%d %d", &num1, &num2);
     int z = add(num1, num2);
     printf("Added value is %d\n", z);
      return 0;
}
int add(int a, int b) 
{
    a += b; 
    return a; 
}

