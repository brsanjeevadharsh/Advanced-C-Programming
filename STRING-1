#include <stdio.h>
int main()
{
    char str1[100];
    printf("Enter the string: ");
   scanf("%[^\n]s",str1);
    int num=0;
    for(int i=0;str1[i]!='\0';i++)
    {
        num++;
    }
    char str2[200];
    for(int i=num-1;i>=0;i--)
    {
        printf("%c",str1[i]);
    }
}
